# --- collection (fetch + parse) ---
requests>=2.31
beautifulsoup4>=4.12
feedparser>=6.0
lxml>=5.0
python-dateutil>=2.9

# Push-only collector: no storage/AI/web deps. The cloud ingest pipeline does
# dedup, summarization, entities, and rendering.

# --- terminal dashboard (dashboard/app.py) ---
# Optional: only needed to run the RawiPress Terminal OS monitoring console.
# Reads local telemetry (sources.json, collector.log, sent-cache, metrics) —
# it does NOT add storage to the collector.
textual>=0.80
rich>=13.7
psutil>=5.9
